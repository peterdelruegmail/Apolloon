name: Update All Standings

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  Heren1:
    uses: ./.github/workflows/Heren1.yml
  Heren2:
    needs: Heren1
    uses: ./.github/workflows/Heren2.yml
  Heren3:
    needs: Heren2
    uses: ./.github/workflows/Heren3.yml
